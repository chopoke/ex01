<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
	  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	  
<mapper namespace="com.spring.mvc_ict05.dao.CustomerDAO">  <!-- namespace="패키지명.인터페이스" -->
	<!-- id 중복확인 -->
	<select id="useridcheck" parameterType="String" resultType="int">
		SELECT COUNT(*) FROM MVC_CUSTOMER_TBL WHERE user_id = #{id}
	</select>
	
	<!-- 회원가입 처리 -->
	<insert id="insertcustomer" parameterType="com.spring.mvc_ict05.dto.CustomerDTO" >
		INSERT INTO MVC_CUSTOMER_TBL(user_id, user_password, user_name, user_birthday, user_address, user_hp, user_email, user_regdate) 
		VALUES 						( #{user_id}, #{user_password}, #{user_name}, #{user_birthday}, #{user_address}, #{user_hp}, #{user_email}, #{user_regdate})
	</insert>
	
	<!-- 로그인 처리 / 회원정보인증 -->
	<select id="idpasswordchk" parameterType="java.util.Map" resultType="int">
		SELECT COUNT(*) FROM MVC_CUSTOMER_TBL WHERE user_id = #{id} AND user_password = #{pw}
	</select>
	
	<!-- 회원정보 인증 처리 및 탈퇴 -->
	<delete id="deletecustomer" parameterType="String">
		DELETE MVC_CUSTOMER_TBL WHERE user_id = #{id}
	</delete>
	
	<!-- 회원정보 인증 상세페이지 처리 -->
	<select id="getcustomerdetail" parameterType="String" resultType="com.spring.mvc_ict05.dto.CustomerDTO">
		SELECT * FROM MVC_CUSTOMER_TBL WHERE user_id = #{id}
	</select>
	
	<!-- 회원정보 수정처리 -->
	<update id="updatecustomer" parameterType="com.spring.mvc_ict05.dto.CustomerDTO">
		UPDATE MVC_CUSTOMER_TBL 
		SET user_password = #{user_password}, user_name = #{user_name}, user_birthday = #{user_birthday}, user_address = #{user_address}, user_hp = #{user_hp}, user_email = #{user_email} 
		WHERE user_id = #{user_id}
	</update>
</mapper>